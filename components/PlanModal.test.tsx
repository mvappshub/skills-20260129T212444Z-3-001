import { describe, it, expect } from 'vitest';
import { PlanModal } from './PlanModal';

describe('PlanModal', () => {
  it('exports the component', () => {
    expect(PlanModal).toBeDefined();
  });
});
